//commentar
#program App;
start
variable integer16 A,A2,B,X,C1,C2;
get(A);
get(B);
if(A >= B) goto Endif;
A2::A;
Next:
if(A2 >= B) goto Endif1;
put(A2 * A2);
A2::A2 add 1;
goto Next;
Endif1:
X::0;
C1::1;
Next1:
if(C1 >= A) goto Endif2;
	C2::1;
	Next2:
	if(C2 >= B) goto Endif3;
		X::X add 1;
		C2::C2 add 1;
	goto Next2;
	Endif3:
C1::C1 add 1;
goto Next1;
Endif2:
put(X);
Endif:
stop